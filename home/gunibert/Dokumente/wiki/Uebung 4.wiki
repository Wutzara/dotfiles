= Uebung 4 =

== Aufgabe 2 ==
Solution

Um einen identischen Baum zu erhalten, müssen wir zwei Bäume separat von-
einander generieren.

Insert(26), Insert(35), Insert(>26), Insert(>26), Insert(1), Extract Min
Wir sollten daraufhin folgenden Baum erhalten haben

      +--+
      |26|
      +--+
     /    \
    v      v
  +--+    +--+
  |40|    |35|
  +--+    +--+
   |
   v
  +--+
  |41|
  +--+
  
Jetzt generieren wir den zweiten Baum.

Insert(24), Insert(46), Insert(47), Insert(48), Insert(1), Extract Min

Unser Baum schaut nun so aus

                   +--+
                   |24|
      .------------+--+--------.
      |              |         |
      v              v         v
    +--+           +--+        +--+
    |26|           |47|        |46|
    +--+           +--+        +--+
   /    \           |
  v      v          v
+--+    +--+       +--+
|40|    |35|       |48|
+--+    +--+       +--+
 |
 v
+--+
|41|
+--+

Nun müssen wir nur noch die nicht benötigten Knoten entfernen. Seiteneffekt:
die 26 wird bei diesem Vorgang markiert.

Delete(41), Delete(40), Delete(48), Delete(47)

Der zweite Baum ist etwas komplizierter. Meine Lösung sieht folgende Eingaben
vor:

Insert(18), Insert(19), Insert(39), Insert(40), Insert(1), ExtractMin
Insert(3), Insert(52), Insert(38), Insert(41), Insert(1), ExtractMin

Das führt zwar nicht direkt zum korrekten Baum - keine Ahnung wie genau das 
gehandhabt wird.

Nun Union(H1, H2), Insert(17), Insert(30), Insert(1), ExtractMin, Insert(23),
Insert(7)


